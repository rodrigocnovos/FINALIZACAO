#!/bin/sh

repo_root="$(git rev-parse --show-toplevel 2>/dev/null)"
if [ -z "$repo_root" ]; then
  exit 0
fi

powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$repo_root/.githooks/bump-launcher-version.ps1"
exit $?
